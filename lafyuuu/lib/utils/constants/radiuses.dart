import 'package:flutter/material.dart';

class AppRadiuses {
  const AppRadiuses._();

  static const BorderRadius a5 = BorderRadius.all(Radius.circular(5));
  static const BorderRadius a216 = BorderRadius.all(Radius.circular(16));
  static const BorderRadius a66 = BorderRadius.all(Radius.circular(3));
  static BorderRadius c5 = BorderRadius.circular(5);
}
